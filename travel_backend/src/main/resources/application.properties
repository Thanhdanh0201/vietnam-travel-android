spring.application.name=travel-backend

# Database - dùng biến môi trường, fallback về giá trị mặc định cho local dev
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres?pgbouncer=true&prepareThreshold=0}
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.datasource.username=${DATABASE_USERNAME:postgres.jfaxmpasqkzztnvckqjl}
spring.datasource.password=${DATABASE_PASSWORD:1266645321@}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT Auth
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=${JWT_JWK_SET_URI:https://jfaxmpasqkzztnvckqjl.supabase.co/auth/v1/.well-known/jwks.json}
spring.security.oauth2.resourceserver.jwt.jws-algorithms=ES256

# Server
server.port=${PORT:8080}
server.address=0.0.0.0
